# All queries that the app may make need to go here
type Query {
  people: [Person]
  books: [Books]
}

type Person {
  name: String
  age: Int
  sex: String
}

type Books {
  title: String
  author: String
}
